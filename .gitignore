# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
.vs/
*.swp
*.swo
*~

# Per-user AI assistant config
CLAUDE.md

# Build / dependency outputs (reserved for future tooling)
bin/
obj/
node_modules/
dist/
build/
*.tsbuildinfo

# bench: generated artifacts (regenerate via scripts in bench/)
bench/dataset/
bench/out/
bench/eval/out/

# API credentials for local eval runs — never commit
.env.local

# cuboidy-snap default output (regenerate via `cuboidy-snap <model-dir>`)
snapshots/
tmp-snap/

# Playwright E2E artifacts (editor package)
test-results/
playwright-report/
.playwright/
