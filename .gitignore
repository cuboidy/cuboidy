# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
.vs/
*.swp
*.swo
*~

# Per-user AI assistant config
CLAUDE.md

# Build / dependency outputs (reserved for future tooling)
bin/
obj/
node_modules/
dist/
build/
*.tsbuildinfo

# API credentials — never commit. Kept even though nothing in this branch
# reads one: the rule costs nothing and the eval branch, which does, was
# briefly one checkout away from committing its keys.
.env.local

# cuboidy-snap default output (regenerate via `cuboidy-snap <model-dir>`)
snapshots/
tmp-snap/

# Playwright E2E artifacts (editor package)
test-results/
playwright-report/
.playwright/

# Git Bash crash dumps
*.stackdump
